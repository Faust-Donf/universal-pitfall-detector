"""无数据时联网检索（接入你的搜索 API）"""


def search_literature(query, num=10):
    """
    调用 ai_search 工具检索文献/资料。
    实际部署时替换为对应 API 调用，这里返回结构化结果占位。
    """
    # results = ai_search(keyword=query, num=num, search_engine="sogou")
    # return parse(results)
    raise NotImplementedError("部署时接入 ai_search 工具")
